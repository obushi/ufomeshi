%div.uk-grid-margin{data: {uk: {grid: "{gutter: 20}"}}}
  - @meals.each do |m|
    .uk-width-small-1-2.uk-width-medium-1-3.uk-width-large-1-3{:style => "width=400px;"}
      .uk-panel.uk-panel-box
        %h2.mq-large
          = l(m.served_on, format: " %Y")
          %span.slash<> /
          = l(m.served_on, format: "%1m")
          %span.slash<> /
          = l(m.served_on, format: "%1d")
          %span.day= l(m.served_on, format: "%a")
          =m.meal_type_i18n
        %h2.mq-small
          = l(m.served_on, format: " %1m")
          %span.slash<> /
          = l(m.served_on, format: "%1d")
          %span.day= l(m.served_on, format: "%a")
          =m.meal_type_i18n
        %ul
          - m.dishes.each do |d|
            %li= d.name
